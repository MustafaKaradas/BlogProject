@using BlogProject.Models.VMs.RoleVM;
@model IEnumerable<RoleListVm>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-controller="Role" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleName)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RoleName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Bu Role Kullanıcı Ekle", "AsignUser","Role" ,new {  RoleId=item.Id  })
                    @*<a asp-area="Admin" asp-controller="Role" asp-action="AsignUser" asp-route-id="@item.Id">Bu Role Kullanıcı Ekle</a>*@
                </td>
            </tr>

        }
    </tbody>
</table>